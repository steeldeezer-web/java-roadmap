package ru.steelDeezer.Day2;

public class AnswerTheQuestions {

    /*## 🎯 ЗАДАЧА 3 — Найди ошибку в рассуждении

---

## Условие

Студент написал:

> *«Когда я создаю объект внутри метода,
после выхода из метода объект сразу удаляется,
потому что стек очищается.
Значит, если я верну ссылку на этот объект,
он всё равно удалится, и программа упадёт.»*

---

## Вопросы (на которые нужно ответить)

### 1. Где ошибка в рассуждении студента?
Если метод возвращает ссылку — объект живёт, потому что ссылка сохраняется в вызывающем коде.
Если ссылка не возвращается и нигде не сохраняется — объект становится недостижимым и позже удаляется GC.
### 2. Напиши код, который опровергает утверждение студента
*/
public static List<String> createList() {
    List<String> list = new ArrayList<>();  // list (ссылка в стеке метода)
    return list;  // ссылка возвращается в main
}   // ← стек метода очищен, но объект в heap жив!

public static void main(String[] args) {
    List<String> myList = createList();  // myList (ссылка в стеке main)
    // myList указывает на ТОТ ЖЕ объект в heap
    // объект НЕ УДАЛЁН, программа работает
}
/*
Код должен:
- Создавать объект внутри метода
- Возвращать этот объект из метода
- Использовать его в `main` **без ошибок**

### 3. Объясни своими словами:
- Что на самом деле происходит с объектом после выхода из метода?
В нашем примере ссылка на объект присваивается переменной myList и объект продолжает жить
- Кто и когда удаляет объекты в Java?
Сборщик мусора когда на объект нет ссылок, при сборке мусора объект будет удален.

}
